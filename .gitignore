# 依赖目录 (Dependencies)
node_modules/
bower_components/

# 构建输出 (Build Output)
dist/
build/
out/

# 日志文件 (Logs)
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 运行时数据 (Runtime Data)
pids/
*.pid
*.seed
*.pid.lock

# 测试覆盖率 (Test Coverage)
coverage/
.nyc_output/

# 编辑器和IDE (Editors & IDEs)
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# 操作系统文件 (OS Files)
Thumbs.db
Desktop.ini

# 临时文件 (Temporary Files)
tmp/
temp/
*.tmp

# 打包文件 (Package Files)
*.zip
*.tar.gz
*.rar

# 用户配置 (User Settings)
.env
.env.local
.env.*.local

# TypeScript 缓存 (TypeScript Cache)
*.tsbuildinfo

# 可选的npm缓存目录 (Optional npm cache)
.npm

# 可选的eslint缓存 (Optional eslint cache)
.eslintcache

# Electron 打包输出 (Electron Package Output)
release/
packages/

# 调试文件 (Debug Files)
*.stackdump
